<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wishwish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wishwish">
        <activity android:name=".HappyResultActivity"></activity>
        <activity android:name=".HappyActivity" />
        <activity android:name=".Blue_Result_Activity" />
        <activity android:name=".BlueActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".DiaryActivity" />
        <activity android:name=".ExplainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>